{
  "name": "invalid-bower-dependencies",
  "version": "1.0.0",
  "description": "A sample project with invalid dependencies to cause errors.",
  "main": "index.js",
  "dependencies": {
    "jquery": "99.99.99",  // Non-existent version of jQuery
    "bootstrap": "4.5.999",  // Non-existent version of Bootstrap
    "non-existent-package": "1.0.0",  // This package doesn't exist in the Bower registry
    "angular": "1.9.0",  // Version that doesn't exist (Angular stopped at 1.8.x)
    "react": "15.0.0",  // Conflicting version, older than current ecosystem versions
    "angular-mocks": "2.0.0"  // Version mismatch (angular-mocks only supports AngularJS 1.x)
  },
  "authors": [
    "Your Name"
  ],
  "license": "MIT"
}
